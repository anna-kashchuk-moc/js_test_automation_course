const a = 6;
const b = 8;

function sum(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

console.log(`The sum of ${a} and ${b} is ${sum(6, 8)}`);
console.log(`The multiplication of ${a} and ${b} is ${multiply(6, 8)}`);
